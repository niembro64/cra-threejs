# Résumé and CV sources

The original August 11, 2026 PDF files are retained locally in `source-pdfs/` and ignored by Git. They are not copied to the public voice-agent knowledge directory.

The adjacent text files are PDF text extractions with direct email addresses and phone numbers replaced by privacy placeholders. Those sanitized files are the versions intended for retrieval.

